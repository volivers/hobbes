<%= render "tabs" %>
<div class="container">
  <div class="row mt-5">
    <div class="col-12">
      <h1><%= current_user.username %></h1>
      <span><%= current_user.location %></span>
      <br>
      <span class="text-lg-left"><%= current_user.bio %></span>
    </div>
  </div>
  <div class="row mt-5">
      <div id="skillset" class="col-6">
        <h2>Skillset</h2>
        <!--TODO if empty show info; else… -->

        <!--TODO loop through by level DESC-->
        <h6 class="badge badge-pill badge-dark">Yoga</h6>
        <h6 class="badge badge-pill badge-info">Chess</h6>
        <h6 class="badge badge-pill badge-success">Hiking</h6>
      </div>
      <div id="goals" class="col-6">
        <h2>Goals</h2>
        <!--TODO if empty show info; else… -->

        <!--TODO loop through by level DESC-->
        <h6 class="badge badge-pill badge-dark">Chess</h6>
        <h6 class="badge badge-pill badge-success">Bread Baking</h6>
      </div>
    </div>
    <div class="notification-actions">
      <%= link_to manage_users_path, class: "btn btn-ghost btn-sm" do%>
          Edit <i class="fas fa-pencil-alt"></i>
      <% end %>
    </div>
    </div>
  </div>
</div>
